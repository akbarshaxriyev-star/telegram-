{
  "name": "telegram-ai-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate && npx prisma db push",
    "start": "cd backend && node -r dotenv/config node_modules/.bin/tsx src/index.ts"
  }
}
